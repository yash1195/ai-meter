# AI Meter website

The static marketing site for AI Meter. It contains no analytics, cookies,
database, server API, or runtime dependency.

## Local preview

```sh
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build

```sh
npm test
```

This validates the static export and rendered page.
